# Ultralytics YOLO 🚀, AGPL-3.0 license
# Default YOLO tracker settings for BoT-SORT tracker https://github.com/NirAharon/BoT-SORT

tracker_type: botsort # tracker type, ['botsort', 'bytetrack']
track_high_thresh: 0.25 # threshold for the first association
track_low_thresh: 0.1 # threshold for the second association (有助于捕捉那些可能因为某些原因（如部分遮挡）而未能在第一次关联中被正确识别的目标)
new_track_thresh: 0.25 # threshold for init new track if the detection does not match any tracks
track_buffer: 100 # buffer to calculate the time when to remove tracks
match_thresh: 0.8 # threshold for matching tracks (较高的值意味着更严格的匹配标准。)
fuse_score: True # Whether to fuse confidence scores with the iou distances before matching
# min_box_area: 10  # threshold for min box areas(for tracker evaluation, not used for now)

# BoT-SORT settings
gmc_method: sparseOptFlow # method of global motion compensation
# ReID model related thresh (not supported yet)
# proximity_thresh、appearance_thresh 分别定义了基于位置接近度和外观特征的阈值，用于确定是否应该将新检测视为已知轨迹的一部分。
proximity_thresh: 0.5
appearance_thresh: 0.25
with_reid: True